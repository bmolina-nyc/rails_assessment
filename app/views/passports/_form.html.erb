

  <%= f.label :home_state %>:
  <%= f.collection_select :state, State.all, :id, :name, :include_blank => true %> 

  <br><br>

  <%= f.label :sign_full_name %>
  <%= f.text_field :full_name %>

  <%= f.hidden_field :user_id, :value => current_user.id %>